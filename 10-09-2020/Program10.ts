// creating an interface

interface Plus{
    addition(FirstNumber, secondNumber);
    
}
 let Calculator:Plus={
     addition(FirstNumber, secondNumber)
     {
         return FirstNumber + secondNumber;
     }

 }
 